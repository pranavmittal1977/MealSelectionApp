import SwiftUI

struct Menu: View {
    

    var body: some View {
        VStack {
            Text("Hello, World!")

            // Button to dismiss the current view
    
        }
    }
}

struct Menu_Previews: PreviewProvider {
    static var previews: some View {
        Menu()
    }
}
